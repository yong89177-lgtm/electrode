# standalone.js 실행 전에 이 파일을 복사해서 .env 로 저장하고 값을 채우세요.
#   cp .env.example .env

# 필수 — 관리자 계정(admin) 최초 비밀번호
ADMIN_PASSWORD=여기에_원하는_비밀번호

# 선택 — 관리자 아이디 (기본값 admin)
#ADMIN_ID=admin

# 선택 — 서버 포트 (기본값 8081)
#PORT=8081

# 선택 — 데이터 저장 폴더 (기본값: 이 프로젝트 폴더 안의 data/)
#DATA_DIR=./data
